# Grafana

Documentation for `structix ops install grafana` command.

Install Grafana dashboard stack using Helm.

## Usage

```bash
structix ops install grafana
```

## Arguments

This command does not take any arguments.

## Options

This command currently has no options.

## Examples

```bash
structix ops install grafana
```