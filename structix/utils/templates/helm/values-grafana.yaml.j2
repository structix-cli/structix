adminPassword: "{{ grafana_admin_password | default('changeme123') }}"

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: "{{ prometheus_datasource_url }}"
        isDefault: true
