replicaCount: 1

image:
  repository: {{ image_repo }}
  tag: {{ image_tag }}
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: {{ port }}

resources: {}

db:
  enabled: {{ db_enabled | lower }}
  type: {{ db if db else "" }}
